import React, { useEffect } from 'react'
import {getAllAppointmentsById,
  addNewAppointments,
  deleteAllAppointmentsById,
  deleteSpecificAppointment,
  getDirections} from '../service/AxiosService';

const DisplayPage = () => {
  useEffect(() => {
    
  }, []);

  return (
    <div>
      display test
    </div>
  )
}

export default DisplayPage
